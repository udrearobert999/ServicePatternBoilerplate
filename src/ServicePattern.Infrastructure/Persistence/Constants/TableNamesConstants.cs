namespace ServicePattern.Infrastructure.Persistence.Constants;

internal static class TableNamesConstants
{
    internal const string Movies = nameof(Movies);
    internal const string Genres = nameof(Genres);
}